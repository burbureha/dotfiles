class Canvas {
  constructor(width, height) {
    this.width = width;
    this.height = height;
  }

  getContext() {
    return null;
  }

  toDataURL() {
    return '';
  }

}

var canvasShim = {
  createCanvas: (width, height) => new Canvas(width, height)
};

export { canvasShim as default };
